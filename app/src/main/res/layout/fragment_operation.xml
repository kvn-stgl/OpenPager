<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>

        <variable
            name="viewModel"
            type="de.openfiresource.falarm.ui.operation.OperationViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:mContext=".OperationFragment">


        <TextView
            android:id="@+id/textview_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{viewModel.operation.title}"
            android:textAlignment="center"
            android:textSize="26sp"
            android:textStyle="bold"
            tools:text="HeaderTest" />

        <TextView
            android:id="@+id/textview_timer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/textview_title"
            android:layout_marginTop="12dp"
            android:text="@{viewModel.timer}"
            android:textColor="@{viewModel.operation.isAlarm ? @color/colorRed : @color/colorBlack}"
            android:textAlignment="center"
            tools:text="TimeTest" />

        <ScrollView
            android:id="@+id/scrollview_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/textview_timer">

            <TextView
                android:id="@+id/textview_message"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="@{viewModel.operation.message}"
                android:textAlignment="center"
                tools:text="ContentTest" />

        </ScrollView>


        <Button
            android:id="@+id/button_confirm_alarm"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:text="@string/operation_confirm"
            android:visibility="@{viewModel.operation.isAlarm ? View.VISIBLE : View.GONE}" />
    </RelativeLayout>
</layout>